// Bundled texture placeholders — components fall back to procedural SVG grain.
// To add real textures: base64-encode a paper texture JPG and assign it here.
// Example: export const paperWarm = "data:image/jpeg;base64,...";
export const paperWarm: string | null = null;
export const paperCool: string | null = null;
